(()=>{const $=s=>document.querySelector(s);async function enrich(){const place=$('#place')?.value.trim();if(!place)return;try{const geo=await fetch('https://geocoding-api.open-meteo.com/v1/search?name='+encodeURIComponent(place)+'&count=1&language=en&format=json').then(r=>r.json()),hit=geo.results?.[0];if(!hit)return;const data=await fetch('https://api.open-meteo.com/v1/forecast?latitude='+hit.latitude+'&longitude='+hit.longitude+'&current=relative_humidity_2m,precipitation,cloud_cover&timezone=auto').then(r=>r.json()),current=data.current;let panel=$('.weather-instruments');if(!panel){panel=document.createElement('section');panel.className='weather-instruments';$('#forecast')?.insertAdjacentElement('beforebegin',panel)}panel.innerHTML='<article><small>HUMIDITY</small><b>'+Math.round(current.relative_humidity_2m)+'%</b></article><article><small>CLOUD COVER</small><b>'+Math.round(current.cloud_cover)+'%</b></article><article><small>PRECIP NOW</small><b>'+Number(current.precipitation||0).toFixed(2)+' in</b></article><article><small>LOCAL CLOCK</small><b>'+new Intl.DateTimeFormat([],{hour:'numeric',minute:'2-digit'}).format(new Date())+'</b></article>'}catch{}}function start(){setTimeout(enrich,900);$('#search')?.addEventListener('submit',()=>setTimeout(enrich,900));setInterval(()=>{const clock=$('.weather-instruments article:last-child b');if(clock)clock.textContent=new Intl.DateTimeFormat([],{hour:'numeric',minute:'2-digit'}).format(new Date())},30000)}if(document.readyState==='loading')addEventListener('DOMContentLoaded',start,{once:true});else start()})();
