document.addEventListener('click',event=>{const card=event.target.closest('#catalog .card');if(!card||event.target.closest('.install'))return;const appId=card.dataset.appId,name=card.querySelector('b')?.textContent;setTimeout(()=>{const body=document.querySelector('#detail-body');if(!body||body.querySelector('.detail-install'))return;const button=document.createElement('button');button.className='detail-install';button.textContent='Install '+name;button.onclick=()=>installApp(appId,name,button);body.appendChild(button);addTerminalInstall(body,appId)},180)});
