addEventListener('DOMContentLoaded',()=>{
  document.body.insertAdjacentHTML('beforeend','<section id="mp-guide" class="mp-guide" hidden><div class="mp-card"><h2>Two-player setup</h2><p>mGBA links both games automatically. The first time, assign a different Xbox controller to each player window.</p><div class="mp-step"><i>1</i><div><b>Player 1 window</b><span>Tools → Settings → Controllers → select controller one.</span></div></div><div class="mp-step"><i>2</i><div><b>Player 2 window</b><span>Open Settings from the second window and select controller two.</span></div></div><div class="mp-step"><i>3</i><div><b>Start versus</b><span>Choose Multiplayer or VS inside both copies of the game.</span></div></div><div class="mp-actions"><button id="mp-cancel">Cancel</button><button id="mp-launch" class="primary">Open linked games</button></div></div></section>');
  const guide=document.querySelector('#mp-guide');document.querySelector('#mp-cancel').onclick=()=>guide.hidden=true;
  window.startVersus=game=>{guide.hidden=false;document.querySelector('#mp-launch').onclick=async()=>{guide.hidden=true;toast('Opening two linked handhelds…');try{const response=await fetch('/api/multiplayer/'+game,{method:'POST'});if(!response.ok)throw new Error();toast('Assign one controller in each mGBA window, then choose VS')}catch{toast('Could not start linked multiplayer')}}};
});
