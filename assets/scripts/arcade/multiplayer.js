addEventListener('DOMContentLoaded',()=>{
  document.body.insertAdjacentHTML('beforeend','<section id="mp-guide" class="mp-guide" hidden><div class="mp-card"><h2>Two-player setup</h2><p>Homebase opens two real mGBA windows for local GBA play. mGBA owns the link cable, controllers, and game windows.</p><div class="mp-step"><i>1</i><div><b>Open the pair</b><span>Homebase launches both copies side by side.</span></div></div><div class="mp-step"><i>2</i><div><b>Make the link</b><span>In the first mGBA window choose File → New Multiplayer Window. Then load the same game if mGBA asks.</span></div></div><div class="mp-step"><i>3</i><div><b>Assign controllers</b><span>In each mGBA window: Tools → Settings → Controllers, then choose a different Xbox controller.</span></div></div><div class="mp-step"><i>4</i><div><b>Start together</b><span>Choose Multiplayer or VS inside both copies of the game.</span></div></div><div class="mp-actions"><button id="mp-cancel">Cancel</button><button id="mp-launch" class="primary">Open two mGBA games</button></div></div></section>');
  const guide=document.querySelector('#mp-guide');document.querySelector('#mp-cancel').onclick=()=>guide.hidden=true;
  window.startVersus=game=>{guide.hidden=false;document.querySelector('#mp-launch').onclick=async()=>{const launch=document.querySelector('#mp-launch');launch.disabled=true;launch.textContent='Opening…';try{const response=await fetch('/api/multiplayer/'+game,{method:'POST',headers:{'X-Homebase-Local':'1'}}),result=await response.json().catch(()=>({}));if(!response.ok)throw Error(result.error||'Could not launch mGBA');guide.hidden=true;toast('Two mGBA games opened · finish the link from File → New Multiplayer Window')}catch(error){toast(error.message)}finally{launch.disabled=false;launch.textContent='Open two mGBA games'}}};
});
