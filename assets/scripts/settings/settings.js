const status=document.querySelector('#status'),theme=localStorage.getItem('nightglass-theme')||'solaris',visual=localStorage.getItem('nightglass-visual')||'0';document.querySelectorAll('[data-theme-choice]').forEach(button=>{button.classList.toggle('active',button.dataset.themeChoice===theme);button.onclick=()=>{localStorage.setItem('nightglass-theme',button.dataset.themeChoice);location.reload()}});document.querySelectorAll('[data-visual-choice]').forEach(button=>{button.classList.toggle('active',button.dataset.visualChoice===visual);button.onclick=()=>{localStorage.setItem('nightglass-visual',button.dataset.visualChoice);localStorage.setItem('homebase-visual-version','3');button.parentElement.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===button));status.textContent='Visual saved. Return to Homebase to see it.'}});const preferences=()=>Object.fromEntries(Object.keys(localStorage).filter(key=>key.startsWith('nightglass-')||key.startsWith('homebase-')||key.startsWith('radio-')).map(key=>[key,localStorage.getItem(key)]));document.querySelector('#export').onclick=async()=>{const areas=[...document.querySelectorAll('.checks input:checked')].map(x=>x.value).filter(x=>x!=='preferences');status.textContent='Building private backup…';const response=await fetch('/api/backup/export',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({areas,preferences:document.querySelector('input[value=preferences]').checked?preferences():{}})});if(!response.ok){status.textContent='Export failed.';return}const a=document.createElement('a');a.href=URL.createObjectURL(await response.blob());a.download='homebase-backup-'+new Date().toISOString().slice(0,10)+'.zip';a.click();URL.revokeObjectURL(a.href);status.textContent='Backup exported to Downloads.'};const picker=document.querySelector('#backup');document.querySelector('#import').onclick=()=>picker.click();picker.onchange=async()=>{const file=picker.files[0];if(!file||!confirm('Merge this Homebase backup into this device? Existing files with the same names may be replaced.'))return;status.textContent='Restoring backup…';const response=await fetch('/api/backup/import',{method:'POST',body:file}),result=await response.json();if(!response.ok){status.textContent='Import failed: '+(result.error||'invalid archive');return}Object.entries(result.preferences||{}).forEach(([key,value])=>localStorage.setItem(key,value));status.textContent='Restored '+result.restored.join(', ')+'. Backup remembers '+(result.installed_apps||[]).length+' installed apps. Reload Homebase to apply preferences.'};
