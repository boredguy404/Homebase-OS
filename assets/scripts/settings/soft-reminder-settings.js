(()=>{const KEY='homebase-soft-reminder-v1',$=id=>document.querySelector(id);const minutes=$('#soft-reminder-minutes'),status=$('#soft-reminder-status');if(!minutes||!status)return;let state={enabled:false,minutes:4.5,nextAt:0};try{state={...state,...JSON.parse(localStorage.getItem(KEY)||'{}')} }catch{}minutes.value=state.minutes||4.5;const render=()=>{const m=Number(minutes.value)||4.5;if(!state.enabled){status.textContent=`Off · will repeat every ${m} minutes when started.`;return}const left=Math.max(0,Math.ceil((Number(state.nextAt)-Date.now())/1000));status.textContent=`On · soft chime every ${m} minutes · next in ${Math.floor(left/60)}:${String(left%60).padStart(2,'0')}.`};const send=(action)=>{const m=Math.max(.5,Number(minutes.value)||4.5);if(action==='start')state={enabled:true,minutes:m,nextAt:Date.now()+m*60000};if(action==='stop')state={enabled:false,minutes:m,nextAt:0};localStorage.setItem(KEY,JSON.stringify(state));if(parent!==window)parent.postMessage({type:'homebase-soft-reminder',action,minutes:m},'*');else window.homebaseSoftReminder?.control(action,m);render()};$('#soft-reminder-start').onclick=()=>send('start');$('#soft-reminder-stop').onclick=()=>send('stop');$('#soft-reminder-test').onclick=()=>send('test');minutes.oninput=render;setInterval(()=>{try{state={...state,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{}render()},1000);render()})();
