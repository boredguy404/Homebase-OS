(()=>{function attach(){const panel=document.querySelector('#assistant-window');if(!panel||panel.dataset.dragReliable)return;panel.dataset.dragReliable='true';panel.classList.remove('locked');localStorage.setItem('homebase-fortify-locked','false');const head=panel.querySelector('header');if(!head)return;let drag=null;head.addEventListener('pointerdown',event=>{if(event.target.closest('button'))return;const box=panel.getBoundingClientRect();drag={x:event.clientX,y:event.clientY,left:box.left,top:box.top};panel.style.right='auto';panel.style.bottom='auto';head.setPointerCapture?.(event.pointerId);event.preventDefault()},true);head.addEventListener('pointermove',event=>{if(!drag)return;panel.style.left=Math.max(8,Math.min(innerWidth-panel.offsetWidth-8,drag.left+event.clientX-drag.x))+'px';panel.style.top=Math.max(30,Math.min(innerHeight-panel.offsetHeight-8,drag.top+event.clientY-drag.y))+'px'},true);const end=()=>{if(!drag)return;drag=null;localStorage.setItem('homebase-relay-position',JSON.stringify({left:panel.offsetLeft,top:panel.offsetTop}))};head.addEventListener('pointerup',end,true);head.addEventListener('pointercancel',end,true);try{const saved=JSON.parse(localStorage.getItem('homebase-relay-position')||'null');if(saved){panel.style.left=saved.left+'px';panel.style.top=saved.top+'px';panel.style.right='auto';panel.style.bottom='auto'}}catch{}setInterval(()=>panel.dispatchEvent(new Event('pointerdown')),3000)}new MutationObserver(attach).observe(document.documentElement,{childList:true,subtree:true});attach()})();
