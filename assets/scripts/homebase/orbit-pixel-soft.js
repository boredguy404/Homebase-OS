/* Final Pixel Field pass. Both compact sizes share the same soft-blue CRT face. */
(()=>{let signal={energy:.08,bass:.05,treble:.04,beat:0};addEventListener('message',event=>{if(event.data?.type==='orbit-energy')signal={...signal,...(event.data.signal||{})}});const unit=v=>Math.max(0,Math.min(1,Number(v)||0));function draw(time){const player=document.querySelector('#orbit-player:not(.expanded)'),canvas=player?.querySelector('.orbit-mini-canvas'),visual=player?.dataset.visual||localStorage.getItem('radio-visual');if(canvas&&(visual==='pixel'||visual==='pixel-dense')){const box=canvas.getBoundingClientRect(),ratio=Math.min(devicePixelRatio||1,2),w=Math.max(1,Math.round(box.width)),h=Math.max(1,Math.round(box.height));if(canvas.width!==w*ratio||canvas.height!==h*ratio){canvas.width=w*ratio;canvas.height=h*ratio}const ctx=canvas.getContext('2d'),retro=document.documentElement.dataset.theme==='ultra-retro',e=unit(signal.energy),bass=unit(signal.bass),treble=unit(signal.treble),beat=unit(signal.beat),cell=3;ctx.setTransform(ratio,0,0,ratio,0,0);ctx.globalAlpha=1;ctx.fillStyle=retro?'#a9c5d4':'#0b1a25';ctx.fillRect(0,0,w,h);ctx.fillStyle=retro?'rgba(255,255,255,.18)':'rgba(139,210,235,.12)';ctx.fillRect(0,0,w,h);const threshold=.55-e*.30-bass*.18-beat*.1;for(let y=0;y<h;y+=cell)for(let x=0;x<w;x+=cell){const f=e+bass*Math.sin(x*.11+time*.004)+treble*Math.cos(y*.16-time*.006)+beat*Math.sin((x+y)*.06+time*.012);if(f>threshold+((x/cell+y/cell)%7)*.012){ctx.globalAlpha=Math.min(1,.32+(f-threshold)*1.5);ctx.fillStyle=retro?'#102b3b':'#83d9f5';ctx.fillRect(x+1,y+1,cell-1,cell-1)}}ctx.globalAlpha=1}requestAnimationFrame(draw)}requestAnimationFrame(draw)})();
