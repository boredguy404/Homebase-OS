(()=>{const clock=document.querySelector('#clock');if(!clock)return;function draw(){const now=new Date(),time=new Intl.DateTimeFormat([],{hour:'numeric',minute:'2-digit',second:'2-digit'}).format(now),date=new Intl.DateTimeFormat([],{weekday:'short',month:'short',day:'numeric'}).format(now);clock.innerHTML=`<b class="clock-time">${time}</b><i class="clock-live" aria-hidden="true"></i><span class="clock-date">${date} · local</span>`;clock.style.setProperty('--second-progress',(now.getSeconds()/60*100)+'%');clock.dateTime=now.toISOString()}draw();setInterval(draw,1000)})();
