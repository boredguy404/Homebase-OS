(()=>{const $=s=>document.querySelector(s),esc=value=>{const node=document.createElement('span');node.textContent=String(value||'');return node.innerHTML},title=path=>String(path).split('/').pop().replace(/\.(md|txt|json|ya?ml)$/i,'').replace(/[-_]+/g,' '),category=path=>path.startsWith('brain/')?path.split('/')[1]||'notes':path.startsWith('docs/')?'guides':path.startsWith('local/')?'local import':'notes';let entries=[];
const request=async path=>{const response=await fetch(path,{cache:'no-store'}),data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data.error||'Local Brain is unavailable');return data};
const render=()=>{const term=$('#search').value.trim().toLowerCase(),items=entries.filter(item=>(item.path+' '+item.id+' '+item.source).toLowerCase().includes(term)),notes=$('#notes');$('#count').textContent=items.length+' note'+(items.length===1?'':'s');notes.innerHTML='';const groups=new Map();items.forEach(item=>{const key=category(item.path);if(!groups.has(key))groups.set(key,[]);groups.get(key).push(item)});for(const[key,group]of groups){const heading=document.createElement('b');heading.textContent=key.toUpperCase()+' · '+group.length;notes.append(heading);group.forEach(item=>{const button=document.createElement('button');button.type='button';button.innerHTML='<span>'+esc(title(item.path))+'</span><small>'+esc(item.path)+' · '+esc(item.source==='Optional local import'?'LOCAL IMPORT':'BUILT IN')+'</small>';button.onclick=()=>open(item,button);notes.append(button)})}if(!items.length)notes.textContent='No notes match that search.'};
const open=async(item,button)=>{document.querySelectorAll('#notes button').forEach(node=>node.classList.toggle('active',node===button));const reader=$('#reader');reader.classList.add('loading');reader.querySelector('small').textContent=(item.source||'LOCAL').toUpperCase();reader.querySelector('h2').textContent=title(item.path);reader.querySelector('p').textContent=item.path+' · loading…';reader.querySelector('pre').textContent='';try{const data=await request('/api/relay/knowledge/'+encodeURIComponent(item.id));reader.querySelector('p').textContent=data.path+' · '+(data.source||'Local note');reader.querySelector('pre').textContent=data.content}catch(error){reader.querySelector('pre').textContent=error.message}finally{reader.classList.remove('loading')}};
const boot=async()=>{$('.back').onclick=()=>parent!==window&&parent.closePocket?parent.closePocket():history.back();$('#search').oninput=render;try{const [knowledge,harness]=await Promise.all([request('/api/relay/knowledge'),request('/api/relay/harness')]);entries=(knowledge.entries||[]).sort((a,b)=>a.path.localeCompare(b.path));render();const cards=[['INDEXED NOTES',harness.knowledge.entries],['LOCAL IMPORTS',harness.knowledge.imported],['SAFE TOOLS',harness.tools.offline_operator],['CORE FILES',harness.tools.core_files],['LEDGER EVENTS',harness.ledger.entries]];$('#harness').innerHTML=cards.map(([label,value])=>'<span><small>'+esc(label)+'</small><b>'+esc(value)+'</b></span>').join('')+'<p>'+harness.boundaries.map(esc).join(' ')+'</p>'}catch(error){$('#harness').textContent=error.message}};addEventListener('DOMContentLoaded',boot,{once:true})})();
