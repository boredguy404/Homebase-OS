/* Local server-backed work board. The live chat reports only recorded work. */
(()=>{
  const key='homebase-kanban-v1',interval=700;
  const stamp=time=>new Date((time||Date.now()/1000)*1000).toLocaleTimeString([],{hour:'numeric',minute:'2-digit',second:'2-digit'});
  const plain=value=>String(value||'No new update yet.').replace(/\b(Codex|API|endpoint|cache|syntax|runtime)\b/gi,word=>({codex:'Relay',api:'connection',endpoint:'route',cache:'local copy',syntax:'code check',runtime:'local app'}[word.toLowerCase()]||word));
  const mergeTasks=remote=>{let local=[];try{local=JSON.parse(localStorage.getItem(key)||'[]')}catch{}const personal=local.filter(task=>!String(task.id||'').startsWith('project-'));localStorage.setItem(key,JSON.stringify([...(remote||[]),...personal]));window.dispatchEvent(new Event('homebase-kanban-refresh'))};
  const boot=()=>{
    const board=document.querySelector('#utility-kanban');if(!board||board.dataset.liveProjectFeed)return false;board.dataset.liveProjectFeed='true';
    document.head.insertAdjacentHTML('beforeend','<style>.project-board-stamp{margin:0 0 9px!important;color:#8fe6ef!important;font:700 10px ui-monospace,monospace!important;letter-spacing:.11em}#utility-kanban .kanban-card[data-task^="project-"]{cursor:pointer}.relay-build-log{margin:16px 0 0;padding:12px;border:1px solid #ffffff25;border-radius:10px;background:#081115}.relay-build-log h3{margin:0 0 7px;font:700 11px ui-monospace,monospace;letter-spacing:.1em;color:#a9f0f5}.relay-build-log p{margin:0 0 10px;color:#b9d5d8;font-size:11px}.relay-build-events{display:grid;gap:6px;max-height:205px;overflow:auto}.relay-build-event{display:grid;grid-template-columns:auto 1fr;gap:8px;padding:7px 8px;border-left:2px solid #72dce7;background:#ffffff08;color:#d5eff1;font-size:11px}.relay-build-event time{color:#8de7f0;font:10px ui-monospace,monospace}.relay-build-event span{overflow-wrap:anywhere}.relay-build-events .fresh{animation:relayUpdateIn .22s ease-out}@keyframes relayUpdateIn{from{opacity:.35;transform:translateY(-3px)}to{opacity:1;transform:none}}html[data-theme="ultra-retro"] .project-board-stamp{color:#000080!important}html[data-theme="ultra-retro"] .relay-build-log{border:2px inset #eee;border-radius:0;background:#fff;color:#111}html[data-theme="ultra-retro"] .relay-build-log h3{color:#000080}html[data-theme="ultra-retro"] .relay-build-log p{color:#111}html[data-theme="ultra-retro"] .relay-build-event{border-left-color:#000080;background:#eee;color:#111}html[data-theme="ultra-retro"] .relay-build-event time{color:#000080}@media(prefers-reduced-motion:reduce){.relay-build-events .fresh{animation:none}}</style>');
    const note=board.querySelector('small');if(note)note.textContent='Tap a project card for a clear status. Personal cards stay only in this browser.';
    const title=board.querySelector('h2'),projectStamp=document.createElement('small');projectStamp.className='project-board-stamp';projectStamp.textContent='RELAY UPDATE FEED · CONNECTING';title?.after(projectStamp);
    const log=document.createElement('section');log.className='relay-build-log';log.innerHTML='<h3>RELAY BUILD CHAT</h3><p>Short, plain updates from real work on this device. It checks for new activity several times per second; nothing is invented when work is quiet.</p><div class="relay-build-events" aria-live="polite" aria-relevant="additions text"><div class="relay-build-event"><time>…</time><span>Connecting to the local work feed…</span></div></div>';board.append(log);
    let lastEvent='';
    const render=feed=>{mergeTasks(feed.tasks);projectStamp.textContent='RELAY UPDATE FEED · checked '+stamp(feed.updated)+' · every 0.7 seconds';const events=log.querySelector('.relay-build-events'),signature=(feed.events||[]).map(event=>event.time+'|'+event.message).join('\n');if(signature===lastEvent)return;lastEvent=signature;events.replaceChildren();(feed.events||[]).slice(-10).reverse().forEach((event,index)=>{const row=document.createElement('div');row.className='relay-build-event'+(index===0?' fresh':'');const time=document.createElement('time'),text=document.createElement('span');time.textContent=stamp(event.time);text.textContent=plain(event.message);row.append(time,text);events.append(row)})};
    const poll=async()=>{try{const response=await fetch('/api/project-feed',{cache:'no-store'});if(!response.ok)throw Error();render(await response.json())}catch{projectStamp.textContent='RELAY UPDATE FEED · local server unavailable'}finally{setTimeout(poll,interval)}};poll();if(location.hash==='#project-board')requestAnimationFrame(()=>board.scrollIntoView({block:'start',behavior:'smooth'}));return true;
  };
  const wait=()=>{if(!boot())setTimeout(wait,120)};if(document.readyState==='loading')addEventListener('DOMContentLoaded',wait,{once:true});else wait();
})();
