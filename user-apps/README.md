# User apps

This is Homebase’s safe app area. A folder here is a self-contained app you can add, back up, inspect, or remove without editing the Homebase core.

See [the app maintainability contract](../docs/APP_CONTRACT.md) for the required `app.json` manifest and the semantic taxonomy for larger collections.
